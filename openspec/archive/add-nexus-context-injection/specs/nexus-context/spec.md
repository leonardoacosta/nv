# Capability: Nexus Context

## ADDED Requirements

### Requirement: nexus context
The system MUST implement: Solve with Nexus flow: inject error context into Nexus start_session prompt as /openspec:explore with pre-loaded context.

#### Scenario: Core functionality
**Given** the feature is implemented
**When** the system operates normally
**Then** the feature works as specified in the PRD
