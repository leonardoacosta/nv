# Capability: Session Progress

## ADDED Requirements

### Requirement: session progress
The system MUST implement: Track progress for /apply, /ci:gh --fix, /feature sessions. Parse Nexus events for phase detection. Expose via /api/sessions.

#### Scenario: Core functionality
**Given** the feature is implemented
**When** the system operates normally
**Then** the feature works as specified in the PRD
