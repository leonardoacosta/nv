# Implementation Tasks

## API Batch

- [ ] [2.1] [P-1] Expand gatherContext with calendar and diary sources -- in `packages/daemon/src/features/briefing/synthesizer.ts`, add `calendar: string | null` and `diaryEntries: DiaryEntryItem[]` to `GatheredContext`; add graph-svc `GET http://localhost:4107/calendar/today` fetch and `getEntriesByDate(yesterday)` call to the `Promise.allSettled` array; record `sources_status.calendar` and `sources_status.diary`; each with 10s timeout [owner:api-engineer]
- [ ] [2.2] [P-1] Enrich briefing prompt with new sections -- in `synthesizer.ts`, update `buildBriefingPrompt()` to add "Today's Calendar" section from `context.calendar` and "Overnight Activity" section from `context.diaryEntries` (interaction count, channels, tools used); update `BRIEFING_SYSTEM_PROMPT` to list all six sections: Messages, Obligations, Calendar, Overnight Activity, Memory, Suggested Actions; group messages by channel in prompt [owner:api-engineer]
- [ ] [2.3] [P-1] Add Telegram delivery to briefing runner -- in `packages/daemon/src/features/briefing/runner.ts`, extend `BriefingDeps` to include `telegram: TelegramAdapter | null` and `telegramChatId: string | null`; after DB insert, send briefing content via `telegram.sendMessage(chatId, content, { parseMode: "Markdown", disablePreview: true })`; truncate to 4096 chars with "... view full briefing on dashboard" suffix if needed; catch and log Telegram errors without failing the run [owner:api-engineer]
- [ ] [2.4] [P-1] Add POST /briefing/generate daemon endpoint -- in `packages/daemon/src/http.ts`, add `POST /briefing/generate` route that calls `runMorningBriefing(deps)` and returns `{ id, generated_at }` on success or `500` on error; requires `BriefingDeps` passed into `createHttpApp` via `HttpServerDeps` [owner:api-engineer]
- [ ] [2.5] [P-1] Wire BriefingDeps with telegram and chatId -- in `packages/daemon/src/index.ts`, pass `telegram` adapter and `config.telegramChatId` into `startBriefingScheduler` deps and into `HttpServerDeps` so the generate endpoint can access briefing deps [owner:api-engineer]

## UI Batch

- [ ] [3.1] [P-1] Add dashboard POST /api/briefing/generate route -- create `apps/dashboard/app/api/briefing/generate/route.ts` that proxies to daemon `POST http://localhost:7700/briefing/generate`; return `{ success: true, briefing_id }` on 200, `{ error }` on failure [owner:ui-engineer]
- [ ] [3.2] [P-1] Add "Generate Now" button to briefing page -- in `apps/dashboard/app/briefing/page.tsx`, add a button with Zap icon next to the Refresh button; on click, POST to `/api/briefing/generate`; show loading spinner on button during request; on success call `loadAll()` to refresh; on error show ErrorBanner [owner:ui-engineer]
- [ ] [3.3] [P-2] Add react-markdown rendering to briefing sections -- add `react-markdown` to `apps/dashboard/package.json`; in `BriefingSectionCard`, replace `whitespace-pre-wrap` paragraph with `<ReactMarkdown>` using tailwind prose classes (`prose prose-sm prose-invert max-w-none`); verify bold, lists, links, code blocks render [owner:ui-engineer]
- [ ] [3.4] [P-2] Show next scheduled briefing time -- in `apps/dashboard/app/briefing/page.tsx`, add a helper `getNextBriefingTime()` that returns "Today at 7:00 AM" if current hour < 7, otherwise "Tomorrow at 7:00 AM"; display below the subtitle text in text-ds-gray-700 [owner:ui-engineer]
