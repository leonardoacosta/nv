# Capability: Dashboard Api

## ADDED Requirements

### Requirement: dashboard api
The system MUST implement: REST API endpoints: /api/obligations, /api/projects, /api/sessions, /api/server-health, /api/memory, /api/config.

#### Scenario: Core functionality
**Given** the feature is implemented per PRD FR-9
**When** the system operates normally
**Then** the feature works as specified in the PRD
