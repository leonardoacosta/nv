# Implementation Tasks

## API Batch

- [x] [1.1] [P-1] Add DigestConfig type and TOML parsing -- in `packages/daemon/src/config.ts`, add `DigestConfig` interface (`enabled`, `quietStart`, `quietEnd`, `tier1Hours`, `tier2Day`, `tier2Hour`, `realtimeIntervalMs`, `p0CooldownMs`, `p1CooldownMs`, `p2CooldownMs`, `hashTtlMs` with defaults), add `digest: DigestConfig` to `Config` interface, add `digest?` to `TomlConfig`, parse `[digest]` section in `loadConfig()` with snake_case TOML keys mapped to camelCase [owner:api-engineer]
- [x] [1.2] [P-1] Add [digest] section to config/nv.toml -- in `config/nv.toml`, add `[digest]` section with `enabled = true`, `quiet_start = "22:00"`, `quiet_end = "07:00"`, `tier1_hours = [7, 12, 17]`, `tier2_day = 1`, `tier2_hour = 9`, `realtime_interval_ms = 300000` [owner:api-engineer]
- [x] [1.3] [P-1] Create gather module -- create `packages/daemon/src/features/digest/gather.ts` with `GatherDeps` interface (`pool: Pool`, `logger: Logger`), `GatherResult` type with typed fields per source and `sourcesStatus` map, `gatherDigest(deps)` function that calls `fleetGet(4107, "/outlook/unread")`, `fleetGet(4105, "/teams/chats")`, `fleetGet(4107, "/calendar/today")`, `fleetGet(4107, "/pim/roles")`, `fleetGet(4107, "/ado/builds")` and DB obligations query via `Promise.allSettled`; parse each result into structured items; record `"ok"` / `"unavailable"` / `"empty"` per source [owner:api-engineer]
- [x] [1.4] [P-1] Create classify module -- create `packages/daemon/src/features/digest/classify.ts` with `DigestItem` type (`id`, `source`, `priority: "P0"|"P1"|"P2"`, `title`, `detail`, `actionable`, `sourceId?`), `classifyItems(result: GatherResult)` function applying rules: suppress noreply/notifications/no-reply/mailer-daemon email; P1 for human email, P2 for other; Teams DM unanswered >1h = P1, @mention = P2, else suppress; PIM expiring <2h = P0, else P2; ADO prod failure = P0, non-prod = P2, success = suppress; obligations overdue/deadline-today = P1, else P2; generate deterministic `id` from source + content hash [owner:api-engineer]
- [x] [1.5] [P-1] Create suppress module -- create `packages/daemon/src/features/digest/suppress.ts` with `suppressItems(items: DigestItem[], pool: Pool, config: DigestConfig)` function; read `_digest_meta` memory topic via pool query; compute SHA-256 hash per item; skip items within cooldown window (P0: 30min, P1: 4h, P2: 12h from config); prune hashes older than 48h (hashTtlMs); write updated `_digest_meta` back; return filtered items array [owner:api-engineer]
- [x] [1.6] [P-1] Create format module -- create `packages/daemon/src/features/digest/format.ts` with `formatDigest(items: DigestItem[], tier: "thin"|"weekly")` returning `{ text: string; keyboard: InlineKeyboardMarkup | null }`; render Telegram Markdown V1 with P0/P1/P2 sections; source icons as `[Mail]`, `[Teams]`, `[Cal]`, `[PIM]`, `[ADO]`, `[Ob]`; build inline keyboards per item using `buildKeyboard` from `channels/telegram.ts` with callback data patterns `digest:pim:activate:{roleId}`, `digest:dismiss:{itemId}`, `digest:ob:done:{obId}`, `digest:ob:snooze:{obId}`; truncate to 4096 chars with "... [N more items]" suffix [owner:api-engineer]
- [x] [1.7] [P-1] Create realtime P0 check module -- create `packages/daemon/src/features/digest/realtime.ts` with `checkP0(deps: DigestSchedulerDeps)` function; call only `fleetGet(4107, "/pim/roles")` and `fleetGet(4107, "/ado/builds")`; classify results for P0 only; apply suppression with 30min P0 cooldown; on P0 detection send standalone urgent Telegram message with inline keyboard; return count of sent alerts [owner:api-engineer]
- [x] [1.8] [P-2] Create scheduler module -- create `packages/daemon/src/features/digest/scheduler.ts` with `DigestSchedulerDeps` interface and `startDigestScheduler(deps)` returning `() => void`; three `setInterval` loops: (a) Tier 1 thin at 60s poll checking hour against `tier1Hours` on weekdays with `lastTier1Dates` dedup, (b) Tier 2 weekly at 60s poll checking day=`tier2Day` and hour=`tier2Hour` with `lastTier2Date` dedup, (c) P0 real-time at `realtimeIntervalMs`; Tier 1/Tier 2 respect quiet hours, P0 bypasses; each loop calls gather->classify->suppress->format->send pipeline; Tier 2 calls Agent SDK `query()` for weekly trend synthesis with 200-word summary [owner:api-engineer]
- [x] [1.9] [P-2] Create barrel export and /digest command -- create `packages/daemon/src/features/digest/index.ts` re-exporting `startDigestScheduler` and `DigestSchedulerDeps`; create `packages/daemon/src/telegram/commands/digest.ts` with `/digest` triggering immediate Tier 1 run and `/digest weekly` triggering immediate Tier 2 run; register in Telegram adapter command routing [owner:api-engineer]
- [x] [1.10] [P-1] Wire digest scheduler into daemon -- in `packages/daemon/src/index.ts`, import `startDigestScheduler` from `features/digest/index.ts`; after briefing scheduler block, conditionally start if `config.digest.enabled && telegram !== null`; pass `{ pool, logger: log, telegram, telegramChatId: config.telegramChatId ?? null, config }` as deps; store cleanup function; call it in `shutdown()` [owner:api-engineer]

## Verify

- [x] [2.1] `cd packages/daemon && pnpm typecheck` passes -- zero TypeScript errors [owner:api-engineer]
- [x] [2.2] `cd packages/daemon && pnpm build` passes -- production build succeeds (2 pre-existing errors in embedding-router.ts and diary/writer.ts, unrelated to digest) [owner:api-engineer]
- [ ] [2.3] [user] Manual test: `/digest` Telegram command triggers immediate thin digest with correct formatting and inline keyboards
- [ ] [2.4] [user] Manual test: digest respects quiet hours (no Tier 1/Tier 2 messages during 22:00-07:00)
- [ ] [2.5] [user] Manual test: P0 real-time check fires every 5 minutes and sends immediate notification on PIM expiry or ADO prod failure
- [ ] [2.6] [user] Manual test: content hash dedup suppresses previously-sent items within cooldown window
- [ ] [2.7] [user] Manual test: nothing-changed skip works (no Telegram message when all items are suppressed)
