# Implementation Tasks
<!-- beads:epic:TBD -->

## API Batch

- [x] [1.1] [P-1] Add `StreamEvent` discriminated union type to `packages/daemon/src/brain/types.ts` -- `type StreamEvent = { type: "text_delta"; text: string } | { type: "tool_start"; name: string; callId: string } | { type: "tool_done"; name: string; callId: string; durationMs: number } | { type: "done"; response: AgentResponse }` [owner:api-engineer]
- [x] [1.2] [P-1] Refactor `processMessageStream()` in `packages/daemon/src/brain/agent.ts` to yield `StreamEvent` instead of current `chunk`/`done` union -- rename `chunk` yields to `text_delta`; on `tool_use` content blocks emit `{ type: "tool_start", name: block.name, callId: block.id }` and track in `Map<string, number>` (callId -> startMs); when next `assistant` message arrives after a tool_use (indicating tool completed), emit `{ type: "tool_done", name, callId, durationMs: Date.now() - startMs }` for each resolved call; keep `done` event and diary/dream logic unchanged [owner:api-engineer]
- [x] [1.3] [P-1] Create `packages/daemon/src/channels/tool-names.ts` -- export `humanizeToolName(rawName: string): string` that parses `mcp__<server>__<tool>` pattern; maps server prefixes to names (`nova-teams`->"Teams", `nova-calendar`->"Calendar", `nova-discord`->"Discord", `nova-mail`->"Mail", `nova-contacts`->"Contacts", `nova-ado`->"Azure DevOps", `nova-graph`->"Microsoft Graph", `nova-memory`->"Memory", `nova-meta`->"Meta"); maps tool action prefixes to verbs (`list_`->"Checking", `get_`->"Reading", `search_`->"Searching", `send_`->"Sending", `create_`->"Creating", `update_`->"Updating", `delete_`->"Deleting"); built-in tools map to descriptions (`Read`->"Reading files...", `Write`->"Writing files...", `Bash`->"Running command...", `Glob`->"Searching files...", `Grep`->"Searching files...", `WebSearch`->"Searching the web...", `WebFetch`->"Fetching page..."); unknown tools return "Working..." [owner:api-engineer]
- [x] [1.4] [P-1] Create `packages/daemon/src/channels/stream-writer.ts` -- export `TelegramStreamWriter` class; constructor takes `adapter: TelegramAdapter` and `chatId: string`, generates random non-zero `draftId` (int); internal state: `currentText: string`, `activeTools: Map<string, { name: string; startedAt: number }>`, `lastFlushAt: number`, `draftSupported: boolean | null` (null = untested), `fallbackMessageId: number | null`; methods: `onTextDelta(text)` appends to currentText stripping unclosed markdown at tail, schedules flush; `onToolStart(name, callId)` adds to activeTools with humanized name (import from tool-names.ts), updates status line prefix, schedules flush; `onToolDone(name, callId, durationMs)` removes from activeTools, updates status line with elapsed, schedules flush; `flush()` enforces 300ms throttle -- if draft supported calls `adapter.sendDraft(chatId, draftId, text)`, if draft unsupported uses `adapter.editMessage(chatId, fallbackMessageId, text)` with 1000ms throttle (creates initial placeholder via `adapter.sendMessage` on first flush if fallbackMessageId is null); `finalize(fullText)` sends complete response via `adapter.sendMessage` with Markdown parse mode, splits at 4096-char boundaries (reuse chunking pattern from index.ts), deletes fallback placeholder if one exists; `abort(error)` sends error message and cleans up [owner:api-engineer]
- [x] [1.5] [P-2] Add `sendDraft(chatId: string | number, draftId: number, text: string): Promise<boolean>` to `TelegramAdapter` in `packages/daemon/src/channels/telegram.ts` -- store bot token as private field `private readonly token: string` in constructor; implement raw `fetch("https://api.telegram.org/bot${this.token}/sendMessageDraft", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ chat_id: chatId, draft_id: draftId, text }) })`; return true on 200 OK, false on non-200 or fetch error; cache availability: if first call returns 404 or "method not found" in response, set `private draftApiAvailable = false` and short-circuit future calls to return false; log failures at debug level [owner:api-engineer]
- [x] [1.6] [P-2] Update SSE handler in `packages/daemon/src/http.ts` to handle new `StreamEvent` types -- change `event.type === "chunk"` to `event.type === "text_delta"`; keep wire format as `{ type: "chunk", text }` for backward compatibility with dashboard client (no dashboard changes needed); ignore `tool_start` and `tool_done` events in SSE output (dashboard does not display tool indicators yet) [owner:api-engineer]

## UI Batch

- [x] [2.1] [P-1] Replace blocking `processMessage()` call in Telegram message handler (`packages/daemon/src/index.ts` lines 257-350) with `processMessageStream()` loop -- create `TelegramStreamWriter(telegram!, msg.chatId)` at start of handler; iterate `agent.processMessageStream(msg, history)` events: `text_delta` -> `writer.onTextDelta(event.text)`, `tool_start` -> `writer.onToolStart(event.name, event.callId)`, `tool_done` -> `writer.onToolDone(event.name, event.callId, event.durationMs)`, `done` -> `writer.finalize(event.response.text)` then proceed to conversation save + diary entry + dream increment; remove the typing indicator `setInterval` (lines 260-263) since drafts replace it; keep catch block calling `writer.abort("Sorry, something went wrong.")` [owner:ui-engineer]

## Verify

- [x] [3.1] `cd packages/daemon && pnpm typecheck` passes -- zero TypeScript errors [owner:api-engineer]
- [x] [3.2] `cd packages/daemon && pnpm build` passes -- production build succeeds [owner:api-engineer]
- [ ] [3.3] [user] Manual test: send a message to Nova on Telegram and observe draft messages appearing progressively as the agent processes (text grows incrementally)
- [ ] [3.4] [user] Manual test: when Nova calls an MCP tool (e.g. /teams or ask about calendar), the draft shows humanized tool name with elapsed time ("Checking Teams... (3s)")
- [ ] [3.5] [user] Manual test: final message replaces draft with full Markdown formatting
- [ ] [3.6] [user] Manual test: if `sendMessageDraft` is unavailable, verify fallback to `editMessageText` on a placeholder message (progressive text replacement)
- [ ] [3.7] [user] Manual test: dashboard chat page still works -- SSE stream delivers text chunks and final response correctly
- [ ] [3.8] [user] Manual test: long response (>4096 chars) is split into multiple Telegram messages on finalization
