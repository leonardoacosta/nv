# Capability: Server Health

## ADDED Requirements

### Requirement: server health
The system MUST implement: server_health table, Nexus health endpoint extension, 60s poll and store, /api/server-health endpoint.

#### Scenario: Core functionality
**Given** the feature is implemented
**When** the system operates normally
**Then** the feature works as specified in the PRD
