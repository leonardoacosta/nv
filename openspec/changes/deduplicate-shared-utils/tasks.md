# Implementation Tasks

## API Batch

- [ ] [2.1] [P-3] Create shared Telegram utility module -- create `packages/daemon/src/utils/telegram.ts` exporting `TELEGRAM_MAX_LEN = 4096` and `splitForTelegram(text: string): string[]` with newline-aware chunking (find last `\n` before limit; hard-split at limit if no good newline found at >= 50% of limit; strip leading newline from remaining) [owner:api-engineer]
- [ ] [2.2] [P-3] Migrate TELEGRAM_MAX_LEN callers to shared utility -- in `channels/stream-writer.ts`: remove local `MAX_LEN`, import `TELEGRAM_MAX_LEN` and `splitForTelegram` from `../utils/telegram.js`, replace local `splitMessage()` with `splitForTelegram`, update `flush()` truncation guard; in `features/digest/format.ts`: remove local `TELEGRAM_MAX_LEN`, import shared constant + helper, replace inline truncation in `formatDigest()` and `formatWeeklySynthesis()` with `splitForTelegram(text)[0]` plus existing suffix; in `features/briefing/runner.ts`: remove local constant and `truncateForTelegram()`, import `splitForTelegram`, replace call with `splitForTelegram(content)[0]` plus `DASHBOARD_SUFFIX` [owner:api-engineer]
- [ ] [2.3] [P-3] Remove dead processMessage() method -- in `packages/daemon/src/brain/agent.ts`, delete the `processMessage()` method (lines 90–187); update the JSDoc on `processMessageStream()` to remove the "Streaming variant of processMessage()" reference; in `packages/daemon/src/features/dream/scheduler.ts`, update the stale JSDoc comment at line 48 that references `NovaAgent.processMessage()` to reference `processMessageStream()` [owner:api-engineer]
- [ ] [2.4] [P-3] Remove dotenv import and dependency -- in `packages/daemon/src/config.ts`, remove `import "dotenv/config"` (line 5); in `packages/daemon/package.json`, remove `"dotenv": "^16.4.5"` from dependencies; run `pnpm install` to update lockfile [owner:api-engineer]
