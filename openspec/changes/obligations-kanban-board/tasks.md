# Implementation Tasks
<!-- beads:epic:TBD -->

## UI Batch

- [ ] [1.1] [P-1] Create `apps/dashboard/components/obligations/KanbanCard.tsx` -- compact obligation card for board view: priority bar (left edge), single-line title (`detected_action` truncated), due date with deadline proximity coloring (reuse `getDeadlineProximity` + `DEADLINE_RING` logic), status badge (`STATUS_BADGE` styling), `draggable` attribute with `onDragStart` setting `obligationId` in dataTransfer; on hover show floating action bar (Done/Dismiss/Reassign icon buttons via `getActionsForStatus` + ArrowLeftRight for reassign); click handler calls `onSelect(id)` for inline expand; expanded state shows full detail: source context, execution history notes, obligation-specific activity from `GET /api/obligations/{id}/activity`; only one card expanded at a time (controlled by parent); drag disabled when `window.innerWidth < 1024` [owner:ui-engineer]
- [ ] [1.2] [P-1] Create `apps/dashboard/components/obligations/KanbanLane.tsx` -- swim lane grouped by status; props: `status`, `label`, `obligations[]`, `expandedId`, `onSelect`, `onRefresh`, `onDrop`; renders header with status label, count badge, collapse toggle (ChevronDown/Up); collapsible body with obligation cards sorted by priority; empty state: single-line "No items" at `opacity-50`; drop target: `onDragOver` with `preventDefault`, `onDrop` extracts `obligationId` from dataTransfer and calls `onDrop(obligationId, status)`; active drop highlight: `border-dashed border-2 border-ds-gray-700` [owner:ui-engineer]
- [ ] [1.3] [P-1] Create `apps/dashboard/components/obligations/KanbanColumn.tsx` -- single owner column; props: `owner` ("nova" | "leo"), `obligations[]`, `expandedId`, `onSelect`, `onRefresh`, `onStatusChange`, `onOwnerChange`; renders column header with owner badge (N/L), obligation count, "+" button for inline create; three KanbanLane instances ordered: in_progress, open, proposed_done; `onDrop` handler in each lane calls `onStatusChange(obligationId, newStatus)`; cross-column drops handled by parent [owner:ui-engineer]
- [ ] [1.4] [P-1] Create `apps/dashboard/components/obligations/InlineCreate.tsx` -- inline obligation creation input; props: `owner`, `onCreated`, `onCancel`; renders a text input in the Pending lane header area; Enter submits POST to `/api/obligations` with `{ detected_action: value, owner, status: "open", priority: 2, source_channel: "dashboard" }`; Escape or blur-with-empty cancels and hides; success clears input and calls `onCreated()`; error shows brief inline message below input; height-reveal animation on show/hide [owner:ui-engineer]
- [ ] [1.5] [P-1] Create `apps/dashboard/components/obligations/KanbanBoard.tsx` -- board container; props: `obligations[]`, `onRefresh`, `approachingDeadlineHours`; manages `expandedId` state (single card expanded at a time); splits obligations by owner into nova/leo/other arrays (reuse existing filter logic); renders two KanbanColumn instances side by side in `grid grid-cols-1 lg:grid-cols-2 gap-4`; handles cross-column drag: `onDragOver`/`onDrop` on each column patches owner via `PATCH /api/obligations/{id}` with `{ owner: targetOwner }`; optimistic update with rollback on failure; "Other" owner obligations shown in a collapsed section below the board [owner:ui-engineer]
- [ ] [1.6] [P-1] Rewrite Active tab in `apps/dashboard/app/obligations/page.tsx` -- replace the flat Nova/Leo sections and ActivityFeed sidebar with `<KanbanBoard obligations={open} onRefresh={fetchObligations} approachingDeadlineHours={approachingDeadlineHours} />`; remove `<ActivityFeed />` from Active tab layout (component stays in codebase for other pages); remove `lg:flex-row` two-column layout with `flex-1 lg:w-2/3` and `lg:w-1/3` sidebar; keep ObligationSummaryBar, tabs, page header, error banner, and loading skeleton unchanged [owner:ui-engineer]
- [ ] [1.7] [P-2] Add Kanban keyboard navigation to `apps/dashboard/app/obligations/page.tsx` -- Up/Down moves focus between cards within a lane, Left/Right moves between columns; `d` marks focused card done, `x` dismisses, `r` reassigns (toggles owner nova/leo), `Enter` expands/collapses focused card; reuse `useApprovalKeyboard` pattern with a new `useKanbanKeyboard` hook; focus ring uses `ring-2 ring-ds-gray-700`; keyboard nav only active when Active tab is selected [owner:ui-engineer]
- [ ] [1.8] [P-2] Add drag-and-drop visual feedback styles -- dragged card gets `opacity-50 scale-95` during drag; drop target lane gets `border-dashed border-2 border-ds-gray-700 bg-ds-gray-alpha-100` highlight; smooth transitions via `transition-all duration-150`; add `cursor-grab` on card header, `cursor-grabbing` during drag [owner:ui-engineer]
- [ ] [1.9] [P-3] Add empty board state -- when both columns have zero obligations, render a centered empty state with illustration text: "No active obligations" and two "+" buttons labeled "Assign to Nova" / "Assign to Leo" that open the inline create input in the respective column; replaces the current dead "No obligations assigned to Nova/Leo" text blocks [owner:ui-engineer]

## Verify

- [ ] [2.1] `cd apps/dashboard && pnpm typecheck` passes -- zero TypeScript errors [owner:ui-engineer]
- [ ] [2.2] `cd apps/dashboard && pnpm build` passes -- production build succeeds [owner:ui-engineer]
- [ ] [2.3] [user] Manual test: Active tab shows two-column Kanban board with Nova and Leo columns, each containing In Progress / Pending / Proposed Done swim lanes [owner:ui-engineer]
- [ ] [2.4] [user] Manual test: drag a card from Nova Pending to Leo Pending -- owner changes to Leo via PATCH; card appears in Leo column [owner:ui-engineer]
- [ ] [2.5] [user] Manual test: drag a card from Pending lane to In Progress lane within the same column -- status changes to in_progress via PATCH [owner:ui-engineer]
- [ ] [2.6] [user] Manual test: click "+" on Nova column, type an obligation, press Enter -- new card appears in Nova Pending lane [owner:ui-engineer]
- [ ] [2.7] [user] Manual test: click a card to expand inline -- shows execution history, source context, and obligation activity; previous expanded card collapses [owner:ui-engineer]
- [ ] [2.8] [user] Manual test: hover over a card -- Done/Dismiss/Reassign actions appear on right edge [owner:ui-engineer]
- [ ] [2.9] [user] Manual test: keyboard navigation -- arrow keys move between cards and columns, `d`/`x`/`r`/`Enter` shortcuts work [owner:ui-engineer]
- [ ] [2.10] [user] Manual test: empty board shows centered empty state with create buttons for both owners [owner:ui-engineer]
- [ ] [2.11] [user] Manual test: WebSocket updates -- when daemon changes an obligation status, the card animates to its new lane in real time [owner:ui-engineer]
- [ ] [2.12] [user] Manual test: mobile (< lg breakpoint) -- columns stack vertically, drag is disabled, action buttons work via expanded card [owner:ui-engineer]
