# Implementation Tasks

<!-- beads:epic:nv-c1ih -->

## DB Batch

- [x] [1.1] [P-1] Create `packages/db/src/schema/projects.ts` -- define `projects` pgTable with columns: id (uuid pk), code (text unique not null), name (text not null), category (text not null default 'work'), status (text not null default 'active'), description (text nullable), content (text nullable), path (text nullable), createdAt (timestamp default now), updatedAt (timestamp default now). Export `Project` and `NewProject` inferred types. Add barrel export in `packages/db/src/index.ts` [owner:db-engineer]
- [x] [1.2] [P-1] Create Zod validation schemas in `packages/db/src/schema/projects.ts` -- `projectCategoryEnum` z.enum(['work', 'personal', 'open_source', 'archived']), `projectStatusEnum` z.enum(['active', 'paused', 'completed', 'archived']), `createProjectSchema` z.object({ code, name, category?, status?, path? }), `updateProjectSchema` z.object({ name?, category?, status? }). Export all schemas [owner:db-engineer]
- [x] [1.3] [P-2] Run `pnpm drizzle-kit generate` in packages/db/ to produce migration SQL for the new projects table [owner:db-engineer]
- [x] [1.4] [P-1] Gate: `tsc --noEmit` passes for @nova/db [owner:db-engineer] [beads:nv-cndr]

## API Batch

- [x] [2.1] [P-1] Add TypeScript types to `apps/dashboard/types/api.ts` -- `ProjectCategory` type ('work' | 'personal' | 'open_source' | 'archived'), `ProjectStatus` type ('active' | 'paused' | 'completed' | 'archived'), `ProjectEntity` interface (id, code, name, category, status, description, content, path, obligation_count, active_obligation_count, session_count, last_activity, created_at, updated_at), `ProjectsListResponse` { projects: ProjectEntity[] }, `ProjectExtractionResponse` { projects_updated: number, sources_scanned: string[] }, `CreateProjectRequest` { code, name, category?, path? }, `UpdateProjectRequest` { name?, category?, status? } [owner:api-engineer]
- [x] [2.2] [P-1] Rewrite `apps/dashboard/app/api/projects/route.ts` GET handler -- query projects table with Drizzle, enrich each with obligation counts (from obligations table grouped by project_code), session counts + last startedAt (from sessions table grouped by project), and join memory topics matching 'projects-%'. If table is empty, seed from NV_PROJECTS env var (parse JSON, insert rows with category 'work' and status 'active'), then return seeded rows. Return `ProjectsListResponse` shape. Support optional `?category=` query param filter [owner:api-engineer]
- [x] [2.3] [P-1] Add POST handler to `apps/dashboard/app/api/projects/route.ts` -- validate body with `createProjectSchema` from @nova/db, insert into projects table, return the created `ProjectEntity`. Return 409 if code already exists [owner:api-engineer]
- [x] [2.4] [P-1] Create `apps/dashboard/app/api/projects/[code]/route.ts` with PUT handler -- validate body with `updateProjectSchema`, update projects row matching code param, set updatedAt to now(), return updated `ProjectEntity`. Return 404 if code not found [owner:api-engineer]
- [x] [2.5] [P-1] Create `apps/dashboard/app/api/projects/extract/route.ts` with POST handler -- for each project in DB, run parallel queries: (a) message count + last mention from messages WHERE content ILIKE '%{code}%', (b) session count + last startedAt from sessions WHERE project = code, (c) obligation counts grouped by status from obligations WHERE project_code = code, (d) memory topics from memory WHERE topic LIKE 'projects-%' AND content ILIKE '%{code}%', (e) diary count + last entry from diary WHERE content ILIKE '%{code}%', (f) top 10 distinct senders from messages WHERE content ILIKE '%{code}%'. Assemble structured markdown, update project content + description columns. Return `ProjectExtractionResponse` [owner:api-engineer]
- [x] [2.6] [P-2] Update `apps/dashboard/lib/entity-resolution/project-enrichment.ts` -- deprecate `enrichProjects()` function with a comment pointing to the new DB-backed extraction pipeline in `/api/projects/extract`. Keep the file for backward compat but mark as legacy [owner:api-engineer]
- [x] [2.7] [P-1] Gate: `pnpm build` passes for dashboard [owner:api-engineer] [beads:nv-08k9]

## UI Batch

- [ ] [3.1] [P-1] Create `apps/dashboard/components/ProjectCategoryTree.tsx` -- props: projects (ProjectEntity[]), onSelectProject (code: string) => void. Group projects by category, render collapsible CategoryNode per non-empty category (ChevronDown/ChevronRight toggle, category label, count badge). Within each category, render ProjectCard children. Categories ordered: work, personal, open_source, archived [owner:ui-engineer]
- [ ] [3.2] [P-1] Create `apps/dashboard/components/ProjectCard.tsx` -- props: project (ProjectEntity), onClick () => void. Render surface-card with: FolderOpen icon, project name (text-copy-14 font-medium), 1-line description (text-copy-13 text-ds-gray-900 truncated), status dot (green=active, amber=paused, blue=completed, gray=archived), last activity relative time, badges for open obligation count and session count. Hover state with border highlight [owner:ui-engineer]
- [ ] [3.3] [P-1] Create `apps/dashboard/components/ProjectDetailPanel.tsx` -- props: project (ProjectEntity), onClose () => void, onUpdate (code: string, data: UpdateProjectRequest) => void. Slide-in panel from right (fixed 420px width desktop, full mobile), backdrop overlay click-to-close, X button. Sections: (1) Header with name, editable category dropdown, editable status dropdown, save button, (2) Knowledge doc rendered as prose (text-copy-14, headings, lists), (3) Activity stats row (obligations, sessions, messages), (4) Action buttons: Edit metadata, Re-extract (triggers extraction for this project only) [owner:ui-engineer]
- [ ] [3.4] [P-1] Create `apps/dashboard/components/CreateProjectDialog.tsx` -- modal dialog with form: code (text input, kebab-case validation), name (text input), category (select dropdown), path (optional text input). Submit calls POST /api/projects. Close on success, show inline error on failure [owner:ui-engineer]
- [ ] [3.5] [P-1] Rewrite `apps/dashboard/app/projects/page.tsx` -- remove all existing code (useState for projects/loading/error/search/solveStatus, fetchProjects via apiFetch, mapApiProject, handleSolveWithNexus, ProjectAccordion usage). New implementation: fetch from GET /api/projects on mount, state for projects (ProjectEntity[]), loading, error, search (debounced), filterTab (all/work/personal/open_source/archived), selectedProject (ProjectEntity | null). Header: "Projects" title with FolderOpen icon, subtitle "N projects across M categories", Refresh button (triggers POST /api/projects/extract then re-fetches list), Create button (opens CreateProjectDialog). Filter tabs matching Contacts pattern. Search input with debounce (300ms) filtering by name and code. Render ProjectCategoryTree for filtered projects. Render ProjectDetailPanel when selectedProject is set [owner:ui-engineer]
- [ ] [3.6] [P-2] Delete `apps/dashboard/components/ProjectAccordion.tsx` -- no longer used after page rewrite [owner:ui-engineer]
- [ ] [3.7] [P-2] Remove `ApiProject` and `ProjectsGetResponse` types from `apps/dashboard/types/api.ts` and replace all imports with new `ProjectEntity` and `ProjectsListResponse` types. Also remove `EnrichedProject` interface (replaced by `ProjectEntity`) [owner:ui-engineer]
- [ ] [3.8] [P-1] Gate: `pnpm build` passes for dashboard [owner:ui-engineer] [beads:nv-hg3h]

## E2E Batch

- [ ] [4.1] TypeScript compilation: `pnpm build` passes with no errors across all packages [owner:ui-engineer]
- [ ] [4.2] [user] Manual smoke: navigate to /projects, verify empty state seeds from NV_PROJECTS, click Refresh to trigger extraction, verify knowledge docs populate, test category filters, test project detail panel, test create new project [beads:nv-vpuy]
